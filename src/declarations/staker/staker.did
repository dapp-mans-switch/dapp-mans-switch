type Staker = 
 record {
   amount: nat;
   days: nat;
   name: text;
 };
service : {
  edit: (nat, text, nat, nat) -> (bool);
  insert: (text, nat, nat) -> (nat);
  listAll: () -> (vec Staker) query;
  lookup: (nat) -> (opt Staker) query;
  remove: (nat) -> (bool);
}
